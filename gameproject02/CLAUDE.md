# SCRAP BLITZ — 作業ガイド

近未来ベルトスクロールアクション。Three.js + 2.5D固定カメラ。
**単一HTML + インラインJS** 構成（`index.html` 一つで完結）。

## 設計の北極星

1. **手触り重視・ノイズ最小化** — 完全ローグライトではない。技は基本固定、ランダム性は敵配置・OVERCLOCK・チップドロップのみ。
2. **板野思想** — 全動作に推力理由を持たせる。ジャンプ・方向転換・キャンセル時に噴射演出を必ず入れる。
3. **メカは重い** — ジャンプ初速は控えめ＋長押しブースターで継続推力（重量感）。
4. **永続強化（チップ・CR・キャラ解放）と一時強化（OVERCLOCK）を分離**。
5. **マルチプレイ意識** — OVERCLOCK発動はウェーブクリア時（同期可能タイミング）。

## ランタイム調整パターン

数値の調整はコンソール経由で即反映できる構造にする：

```js
window.SB.PHYSICS.JUMP_V = 12        // 即変更可
window.SB.ATTACKS.z1.damage = 8      // 即変更可
```

新パラメータを足すときは `PHYSICS` / `ATTACKS` 等の定数オブジェクトに追加し、
`window.SB = { PHYSICS, ATTACKS, ... }` に必ず露出させる。

## 変数定義順の鉄則（TDZ事故防止）

`STATE` / `PHYSICS` / `ATTACKS` / `Z_CHAIN` は **`players` 配列の宣言より前**に定義する。
順序ミスで画面真っ黒になった実績あり。新しい定数を足すときも同じ位置に置く。

## 現Phase / 次やること

- ✅ Phase 1: 移動・ジャンプ（ブースター式）・ヨースラスター
- ✅ Phase 2.1: Z弱攻撃3段 / ヒットストップ / シェイク / キャンセルジャンプ
- 🔜 Phase 2.2: 強攻撃X / ダッシュ / 打ち上げ / グラブ / メガクラッシュ / コマンド技 / 敵AI

**Phase 2.2 着手前に未決断🔴を確定させること**（詳細: `~/.claude/plans/buzzing-juggling-sedgewick.md`）：
1. ガード手段（❶ 操作系根幹・後付け困難）
2. 死亡ペナルティ（❷ 現状ゼロで緊張感欠落）
3. チップシステム認知負荷の段階解放
4. ストーリー・世界観の最低限実装
5. コンボパターン固定化対策

特に **❶❷は実装着手前に必ず確定** させる。後から足すと操作系・経済系全体に波及する。

## 廃止済み（蒸し返さない）

ローグライト寄りの旧案は全廃：
- アーティファクト3択 / パーツのムーブセット変化 / メーカーセット効果 / ボスバリエーション抽選 / ライバル乱入

代わりに **OVERCLOCK**（Apex式2択育成）+ **チップシステム**（バックパックヒーロー式）を採用。
ルートキャリアのみ乱入として残存。

## 関連ファイル

- 既存プラン全文: `~/.claude/plans/sharded-doodling-reddy.md`
- 前セッションログ: `~/.claude/plans/scrapblitz-log-2026-05-10.md`
- 引き継ぎファイル: `~/.claude/plans/buzzing-juggling-sedgewick.md`
- メモリ: `~/.claude/projects/G--claude-code-local/memory/project_scrapblitz.md`
